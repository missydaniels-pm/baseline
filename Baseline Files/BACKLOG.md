# Baseline — Product Backlog

Last updated: July 14, 2026 | open self-serve registration

**Priority:** P0 = fix now, P1 = next sprint, P2 = soon, P3 = later
**Size:** S = small (<2hrs), M = medium (half day), L = large (1+ days)

---

## P0 — Fix Now

Privacy/legal foundation items and active bugs. Complete before adding users beyond trusted circle.

| Area | Item | Source | Size | Status |
|---|---|---|---|---|
| Privacy & Legal | Write and publish privacy policy (in app + linked from Settings) | Legal | M | ✅ Complete — /privacy route live, linked from login/register/settings 3/18/26 |
| Privacy & Legal | Build account + data deletion feature in Settings | Legal/MHMD | M | ✅ Complete — /settings/delete-account |
| Auth & Security | Frontend password match validation on registration | Lizz | S | ✅ Complete |
| Auth & Security | Password strength indicator and requirements (register + change password) | Lizz | S | ✅ Complete |
| Data Integrity | Symptom name field: 200 char limit + frontend validation | Lizz | S | ✅ Complete |
| Data Integrity | Description/notes fields: 500 char limit with counter | Lizz | S | ✅ Complete |
| Data Integrity | Enforce unique symptom names per user (case-insensitive) | Lizz | S | ✅ Complete |
| Data Integrity | Prevent future episode dates (frontend + backend) | Lizz | S | ✅ Complete |
| UX | Grey out sidebar nav during onboarding with tooltip | Lizz | S | ✅ Complete |
| Experiments | Lower default experiment duration to 3 weeks | Mackenzie | S | ✅ Complete |
| Experiments | Fix protocol/experiment workflow — inline protocol creation from experiment form, auto-suggest experiment name, active experiment warning modal before submit | Missy | S | ✅ Complete 4/6/26 |
| Data Integrity | Protocol delete 500s on production (IntegrityError — FK children never removed) | Missy smoke test | S | ✅ Complete 7/12/26 — pre-existing bug surfaced by Today's Protocols card writing compliance rows for every active protocol. `delete_protocol` now removes EpisodeIntervention/ProtocolCompliance/ProtocolEvent rows and detaches Experiments (history preserved) before deleting, with IntegrityError rollback. Same hole patched in `/dev/reset` (missing ProtocolEvent). SQLite doesn't enforce FKs, which is why local testing never caught it. |

---

## P1 — Next Sprint

High-value improvements targeting user satisfaction, retention, and portfolio readiness.

| Area | Item | Source | Size | Notes |
|---|---|---|---|---|
| UX | App-wide naming overhaul — rename "Symptoms" to "What I Track", rename "Rescue Medications" to "Interventions", update Protocols description from medication-centric copy to "Ongoing practices, medications, supplements, and routines that support your health". Impacts: nav labels, page headers, onboarding flow, help page, welcome modal, empty states, dashboard headers, CLAUDE.md, README, backlog. | Kiersten/Missy | M | ✅ Complete 4/12/26 |
| Episode Logging | Allow multiple rescue options per episode | Mackenzie | M | ✅ Complete 4/12/26 — EpisodeIntervention junction table, multi-intervention forms, AI prompt updated |
| Tracked Items | Binary (Yes/No) tracked-item type — Spec 01 of 4 | Mackenzie | M | ✅ Complete 6/12/26 — `Symptom.input_type` ('scale'|'binary') with DB CHECK constraint (`symptoms_input_type_check`), `SymptomScore.value_bool` separate column from `score` (filtering `IS NOT NULL` keeps the two types from contaminating each other), type selector on new/edit symptom forms + onboarding step 1, locked once a SymptomScore exists, episode logging renders Yes/No tap targets (tap-again-to-clear, `aria-pressed`, focus-visible outline), baseline-score step skipped for binary in onboarding, AI check-in prompt teaches Claude per-symptom type and accepts boolean values, dashboard binary stat cards (yes/N) + per-episode dot strip, experiment assessment compares yes-rate before/during, episode history shows "Yes" / "No" / "—". |
| Protocol Tracking | Manual compliance logging without AI check-in | Mackenzie | S | ✅ Complete 7/12/26 — dashboard "Today's Protocols" card is the manual path (see row below); per-protocol detail form retained. |
| Protocol Tracking | Rework daily compliance tracking UX (pre-React-rebuild) | Missy | M | ✅ Complete 7/12/26 — dashboard "Today's Protocols" card: assumed-taken pills (dashed style until confirmed), one-tap Confirm writes the whole day via JSON `POST /protocols/log-day`, quiet "Missed all today", optional per-miss notes, calm collapsed state, 7-day tappable dot strip (green/amber/hollow, purple ring today) with inline backfill editor (neutral-default pills, partial saves, guilt-free exit). Supportive messaging personalized from new `Protocol.why` → experiment hypothesis → generic pool. AI check-in reached full parity (records misses + notes, upserts). Designed and iterated in the Claude Design project first. See Decision Log. |
| Protocol Tracking | Today's Protocols — partial logging bug (assumed-taken marked everything) | Missy | S | ✅ Complete 7/14/26 — reported bug: marking one protocol Complete and saving wrote *all* as taken (blank = assumed-taken on confirm), with no way to un-mark. Fix: tri-state pills (Complete / Not Today / blank) with tap-again-to-clear; button reads "Confirm all as complete" only when nothing is marked (keeps one-tap assumed-compliance) and "Save" once anything is marked (saves only what's set, blanks sent as `took=null` → `_delete_compliance()` un-logs the row, even after a prior confirm). Backfill Save also sends tri-state. Pills relabeled Taken/Missed → "Complete"/"Not Today" to drop medication bias. See Decision Log. |
| Protocol Tracking | User timezone support | Missy | M | Railway runs UTC — server `date.today()` rolls over at ~4-5pm Pacific. Interim fix 7/12/26 (browser-local date for writes). **7/14/26 — fixed a real production bug** (see Decision Log): the `baseline_tz` cookie was URL-encoded by base.html but read un-decoded, so `ZoneInfo` always raised → the dashboard render silently used UTC → evening entries showed a day off / appeared to revert cross-device. Fix: shared **`user_today()`** helper (unquotes the cookie) is now the single source of truth for every functional day-boundary path (dashboard render, detail-form compliance write+read, protocol events, AI check-in clamp), + `test_timezone.py`. **Full fix still open:** stored/auto-detected `User.timezone` applied to **chart bucketing**, episode timestamps/`onset` model (which incidentally makes the coarse future-date guard exact), **form date defaults**, and **unifying the card's browser-sent write date with the cookie** (A1 dual-source divergence — accepted at current scale, durable fix is stored `User.timezone`). |
| Episode Logging | Structured trigger tracking | Missy | M | Pre-rebuild. **Increment 1 — data-model pass (7/14/26):** `Trigger` + `EpisodeTrigger` models, two partial unique indexes (`ux_trigger_global_name`, `ux_trigger_user_name`), idempotent 12-item global seed (`SEED_TRIGGERS`), FK-safe deletes in `delete_account`/`dev_reset` + `User.triggers` cascade, `test_triggers.py` (9 cases incl. FK-safe + ORM-cascade delete). No read/write path yet. Reviewed (QA + Code + UX); the one "BLOCKER" (delete_episode cascade) was disproven by test — ORM cascade handles `EpisodeTrigger` exactly like `EpisodeIntervention`. **Increment 2 — episode capture path (7/15/26):** chip multi-select picker + inline "+add" on new/edit episode; `_resolve_trigger()` match-and-link (active global → own custom w/ reactivate → else create, `begin_nested` savepoint against the concurrent same-name race); `_save_episode_triggers_from_form()` (user-scoped, dedup, `source='user'`, replace-on-save on edit with `preserve_ids` so a linked-inactive custom isn't dropped); history shows `⚡ …`. `EpisodeTrigger.source` column added (Code Review A1 resolved — owner-approved add-now). `test_trigger_writes.py` (16 checks). Reviewed QA+Code+UX; 3 blockers found & fixed (IntegrityError escaped the try/except → moved to a savepoint; typed-but-not-added trigger lost → flush on submit; long name overflowed 375px → `overflow-wrap`). **Next increment:** Settings "Manage triggers", AI check-in parity (`source='ai'`), dashboard reads. Add a first-class trigger dimension to episode logging (vs. free-text notes) so triggers become queryable. Unlocks dashboard breakdowns and feeds the P2 AI trigger-analysis item. **Model: hybrid chosen 7/14/26** — curated global seed list + user-defined custom triggers, one `Trigger` table with nullable `user_id` (null = global) + an `EpisodeTrigger` junction (single clean FK). Sub-decisions (7/14/26): (a) **global seed list capped at ≤12 items** (Missy) — forces curation, keeps the picker scannable, and is precisely what lets per-user hide safely defer; (b) **per-user *add* is v1** (create/rename/deactivate your *own* customs — the pressure-release valve for the short global list); **per-user *hide* of globals is v2** (with ≤12 globals the list stays short, so v1 shows all); (c) global↔custom name collision — reuse the `Symptom` case-insensitive-unique precedent; on custom-create (from either surface below), match-and-link to an existing global rather than duplicating (prevents split analytics); (d) **custom deletion = soft-deactivate** (`is_active=False`), never hard-delete, so linked episode history survives (FK-safe; mirrors the soft-retire rule for globals); (e) **two surfaces:** full trigger management lives in **Settings** ("Manage triggers" — rename/deactivate own customs; v2 hide-toggles for globals), keeping the main nav clean, **and** the **episode form gets an inline "+ add"** so a user can create-and-select a new trigger mid-log without leaving the flow (routed through the same match-and-link check). The episode form otherwise shows only the picker (globals + the user's active customs); (f) **forward-only** — no backfill of historical free-text notes. Aggregate/community analytics (P3) will only span globals, not custom triggers. |
| Help & Onboarding | YouTube feature update videos | Missy | S | Pre-rebuild. Record short feature walkthrough/update videos and surface them in-app (Help page) and in update emails. Lightweight — no app changes required to start; embed once videos exist. |
| Help & Onboarding | Email opt-in for product updates | Missy | S | ✅ Complete 4/24/26 — see "Email & Admin" row below and the Email Opt-In decision-log entry. Settings toggle + unsubscribe link + Resend audience sync + privacy policy update shipped together. |
| Dashboard | Chart time range selector (days / weeks / months) | Mackenzie/Missy | M | Build before React rebuild — understand the requirement fully before rebuilding frontend. |
| Help & Onboarding | Full Help page with Dashboard explanation + check-in tutorial | Internal | M | ✅ Complete 3/21/26 |
| Help & Onboarding | Welcome email for new users | Internal | S | ✅ Complete 3/21/26 — uses Gmail SMTP, requires MAIL_USERNAME and MAIL_PASSWORD env vars in Railway |
| Help & Onboarding | Welcome tour modal | Internal | S | ✅ Complete 3/21/26 — has_seen_tour boolean on User model, 5-step slideshow on first login |
| UX | Dashboard check-in shortcut | Internal | S | ✅ Complete 3/21/26 — "Start Check-in →" button in dashboard header next to Log Episode |
| UX | Update contact email to baselinehealthapp@gmail.com | Internal | S | ✅ Complete 3/21/26 — updated in help page and welcome email |
| UX | Remove invite code reference from help page | Internal | S | ✅ Complete 3/21/26 — registration flow may change |
| Auth & Security | Self-serve registration with email verification | Missy | M | ✅ Complete 4/14/26 — itsdangerous signed tokens (24h TTL), SHA-256 replay protection via `used_verify_tokens`, Flask-Limiter rate limits, disposable-email blocklist, privacy-policy acknowledgment checkbox, stale-account cleanup at 48h. Replaces invite-only registration. |
| UX | Dashboard empty states for new users | Internal | M | ✅ Complete 3/18/26 — per-section empty states with SVG placeholders and action links |
| UX | Experiments page empty state with assessment preview | Internal | S | ✅ Complete 3/18/26 — full two-column assessment preview using real assess-*/decision-* classes at 50% opacity |
| Analytics | Internal event logging to PostgreSQL (privacy-safe instrumentation) | Internal | M | ✅ Complete 4/22/26 — UserActivity model (signup/login/page_view events), admin-only /admin/analytics dashboard with DAU/WAU/feature usage/retention, is_admin flag on User, privacy policy updated. |
| Email & Admin | Email opt-in preference + unsubscribe + Resend contacts sync + admin user view | Missy | M | ✅ Complete 4/24/26 — `email_updates_enabled` on User (default True), Settings → Email Preferences auto-save toggle, public `/unsubscribe/<token>` (HMAC-SHA256, no expiry, enumeration-safe, rate-limited 30/hour), welcome email footer with unsubscribe link, Resend audience sync on verify/email-change/preference-toggle/unsubscribe/account-delete (gated on `RESEND_AUDIENCE_ID`, log-and-swallow), `/admin/users` read-only directory (email/verified/joined/last login/email pref/episode count) guarded by `is_admin`. Privacy policy updated to disclose admin access + Resend as email processor. AI Logging toggle also auto-saves; `ANTHROPIC_API_KEY` dev diagnostic removed from user-facing settings. |
| Portfolio | Prepare repo for public GitHub launch — clean dev routes, write README | Internal | M | ✅ Complete — dev routes cleaned 3/5/26, README written, repo public 3/4/26 |
| Reporting | Neurologist insurance report — auto-generated PDF matching standard migraine calendar form. Day, category (M/H/P), pain score 0-10, medication codes, monthly totals. Required for insurance approval of triptans/gepants. Baseline already captures all needed data. | Missy | L | DEFER TO POST-REACT REBUILD — backend logic transfers cleanly, but PDF generation layer will be cleaner in API-first architecture. Spec the backend now, build properly during React rebuild. |

---

## Deferred to React Rebuild

Items that would create throwaway frontend work if built in the monolith. Build during or after React/React Native rebuild.

| Area | Item | Source | Size | Notes |
|---|---|---|---|---|
| UX | Configurable check-in reminders | Internal | M | Defer — push notification infrastructure is cleaner in React Native with proper notification APIs |
| UX | Light mode | Internal | M | Defer — easier to implement properly in React with CSS variables than retrofit Jinja2 templates |
| Episode Logging | Photo logging | Mackenzie | L | Defer — React Native territory. Do not build in monolith. |
| AI Features | Async AI check-in (submit → poll/stream job pattern) | Missy/Code Review | M | **Rule 2 (slow work off request path) deviation — interactive variant.** The check-in makes the Anthropic call synchronously in-request, and the user *is* waiting for the parsed result, so it can't be fire-and-forget. Under load this risks tying up / timing out gunicorn workers. Durable fix is an async job pattern (POST submits → returns a job id → client polls or streams → render when ready) — a natural fit for the React rebuild's API-first architecture, not a monolith retrofit. **Not a Rule 1 (statelessness) violation** — the request leaves no per-server state. Distinct from the future P2 "AI trigger analysis" item, which is batch (nobody waiting) and is true background-worker territory. |
| Auth & Security | Web-client API auth model (CSRF posture for the rebuild) — **rebuild scope** | Missy/Code Review | M | Architecture decision, then build in the rebuild. Decide: header-token auth (token in memory/`Authorization` header — removes browser CSRF but adds an XSS-management responsibility) **vs.** cookie sessions + `SameSite=Strict/Lax` + CSRF tokens. This determines whether the React **web app / PWA** has any CSRF exposure at all. Native App Store (React Native) apps use header-token auth and are **not** CSRF-exposed regardless of this choice. **Independent of — and does NOT supersede — the P2 monolith Flask-WTF item:** the PWA stays for current users, so a browser CSRF surface persists until this is explicitly designed out. Raise trade-offs (CSRF vs. XSS) before building. |

---

## P2 — Soon

Important but not urgent. Build once P0 and P1 are clear.

| Area | Item | Source | Size | Notes |
|---|---|---|---|---|
| Dashboard | Include current partial week in trend charts | Lizz | S | ✅ Resolved — current week now shows with asterisk label |
| AI Features | Trigger analysis: AI surfaces patterns from episode notes on dashboard | Mackenzie | L | Episode notes already capture trigger text |
| Analytics | PostHog self-hosted session recording | Internal | M | After privacy policy live and MHMD review complete |
| Infrastructure | Staging environment on Railway | Internal | M | Separate branch, deploy before main |
| Infrastructure | GitHub Actions: automated doc updates on deploy | Internal | M | Phase 1 of automated documentation pipeline |
| Infrastructure | GitHub Actions: basic automated testing | Internal | M | |
| Privacy | Consult health tech lawyer — Washington MHMD obligations | Legal | S | Before paid tier or significant user growth |
| Privacy & Legal | Update privacy policy to describe transactional email (verification + welcome) and temporary storage of unverified accounts (48h TTL) | Internal | S | ✅ Complete 4/14/26 — `templates/privacy.html` updated (in-app page is single source of truth). |
| Auth & Security | Add CSRF protection (Flask-WTF) to all auth forms — **monolith scope** | Code Review | S | Pre-existing gap surfaced during self-serve reg review. `SameSite=Lax` (browser default) mitigates most cross-site POSTs; rate limiting mitigates further — but neither is a full defense. With open registration now live, also covers `/settings/email-preferences`, `/settings/email`, `/settings/password`, `/settings/delete-account`, and the JSON `POST /protocols/log-day` compliance endpoint (7/12/26). **Not superseded by the React rebuild** — protects users for the entire (multi-month) monolith window and any post-rebuild web/PWA surface that keeps cookie sessions. Independent of the rebuild-scoped auth-model item under "Deferred to React Rebuild." Do now. |
| AI Features | Trend-based supportive messages on the compliance card | Missy | M | Fast-follow to the 7/12/26 compliance card. Surface real data trends ("episodes down N% since starting {protocol}", "logging rate up this month") in the same calm message slot. Rules agreed in design: only when statistically noticeable, tied to a concrete action, no confetti, never a push notification. Start with the two cheap ones (logging-rate delta, longest validated stretch — worded without "streak"). |
| Protocol Tracking | Exact historical protocol status for backfill | Internal | S | Backfill editor currently approximates "active on that day" as `start_date <= day` for currently-active protocols. Exact answer requires replaying ProtocolEvent (paused/stopped/reactivated) — fine to defer at a 7-day window. |
| UX | Unify Taken/Missed pill styling between dashboard card and protocol detail | UX Review | S | Two visual languages for the same action shipped 7/12/26: `.tp-pill` (dashboard, filled color + dashed assumed state) vs `.proto-log-label` radio pills (protocol detail). Restyle the detail-page log form to reuse `.tp-pill`, or document why it stays lightweight. |
| Experiments | Compliance-aware experiment views — show adherence % for the protocol during the experiment window (e.g. "logged taken 84% of stabilization days") | Missy | M | 7/12/26 smoke test. Now that the dashboard card makes compliance data dense, experiment assessment can show real adoption of the new protocol. Decide how unlogged days count (unknown vs assumed) — same question as the deferred trend-messaging item; consider scoping together. |
| Protocol Tracking | "Date stopped" field when pausing/stopping a protocol | Missy | S | 7/12/26 smoke test. ProtocolEvent already records pause/stop events, but dated the day the user clicked — not the day the protocol actually stopped. Add an effective-date field to the status-change flow so retroactive stops correlate correctly with episode data. |
| Protocol Tracking | Richer delete protection for preventatives with compliance history — informed confirmation with history count ("47 days of history will be deleted") or soft-delete/archive pattern like rescue interventions | Code Review | S | 7/12/26 — the FK-safe delete fix made silent history loss newly *possible* (previously the delete just crashed). Interim mitigation shipped: confirm dialog states history is permanently deleted. Decision pending with Missy: informed count vs archive. |
| Episode Logging | Triggers — deferred review findings (7/15/26 capture-path pass) | QA/Code/UX | S | Accepted-not-fixed items, each low-risk at current scale: (a) **non-ASCII case-fold** — match-and-link + the partial unique indexes use SQL `lower()`, which folds ASCII only in SQLite (Python `.lower()` folds Unicode), so "CAFÉ" vs "café" can duplicate a custom locally; Postgres locale may differ. Same limitation as `Symptom` name-uniqueness. Fix needs `citext`/normalized column across engines. (b) **`source` has no DB CHECK** ('user'/'ai') unlike `symptoms.input_type` — add the constraint when the AI trigger-write path lands. (c) **N+1** on the episodes history list (`ep.episode_triggers.trigger` lazy per row — same pattern as interventions); eager-load if `/episodes` ever paginates. (d) **No ✕-remove** for a session-added custom chip (can only uncheck; unchecked = not saved, so no data pollution, just visual clutter). (e) **No `<fieldset>`/`<legend>`** on the chip group + `⚡` emoji-only history marker lack SR text — roll into a broader a11y pass across grouped controls (binary toggle has the same gap). |
| Protocol Tracking | Edit existing compliance entries older than 7 days from the History timeline | Missy | S | 7/12/26 — currently entries older than the dashboard's 7-day backfill window are frozen (History timeline is read-only; detail form is today-only). Add an edit affordance on taken/missed timeline entries opening the Taken/Missed + note form, posting through `_upsert_compliance` with a relaxed date window for rows that already exist. Deliberate boundary kept: correcting an existing record is allowed at any age, but *creating* entries for blank days stays capped at 7 days (memory-based guessing corrupts experiment data — a blank day is honest data). |
| Auth & Security | Switch Flask-Limiter to Redis backend once Railway Redis is provisioned | Code Review | S | **Rule 1 (statelessness) deviation.** In-memory backend resets on each deploy and is per-worker — acceptable at current single-instance scale but a documented gap. Shares the Redis provisioning with the email-worker item below — do them together (provision Redis once, close both). |
| Infrastructure | Provision Railway Redis + move transactional email off the request path | Missy/Code Review | M | **Rule 2 (slow work off request path) deviation — email.** Verification/welcome Resend calls run synchronously in-request today; they're fire-and-forget (the user doesn't wait for them), so defer to a background job (RQ/Celery) on Redis and let the request return immediately. **Shares infra with the Flask-Limiter → Redis item above:** one Redis provision closes both the Rule 1 rate-limiter gap and this Rule 2 email gap; sequence Redis first. Latent at single-instance/low load — a latency/throughput concern, not a correctness bug. The worker/queue must be shared (Redis) — an in-process thread would fix Rule 2 but create a Rule 1 violation. |
| Architecture | Move FK cleanup from hand-ordered route code to DB-level `ondelete` (CASCADE for owned children, SET NULL for detachable refs) with `passive_deletes` | Code Review | M | 7/12/26 — every delete route currently hand-orders child cleanup; manual auditing missed two paths (found by review). DB-level `ondelete` makes integrity an enforced invariant instead of a checklist. Needs a schema migration (SQLite table rebuilds). Deliberate design pass pre-React-rebuild, not piecemeal. |

---

## P3 — Later

Longer-term vision. Architecture decision point: React rebuild is the gateway to native apps.

| Area | Item | Source | Size | Notes |
|---|---|---|---|---|
| Architecture | React frontend rebuild (API-first, positions for React Native) | Internal | L | Planned post-vacation (April/May 2026) when full time. Deliberate learning project, not just a refactor. |
| Architecture | MCP server layer — expose Baseline as agent-accessible backend | Internal | L | Build during or after React rebuild. API-first architecture makes this natural. See Decision Log. |
| Platform | React Native iOS + Android apps | Internal | L | Follows React rebuild — not a parallel track |
| Platform | Apple Health / HealthKit integration | Mackenzie | L | Requires native iOS app |
| Platform | HRV correlation with episodes | Missy | L | Post-rebuild. Pull HRV from Apple Health and correlate against episode onset/severity — surface as a dashboard signal. Depends on HealthKit integration above. |
| Platform | Blood glucose correlation with episodes | Missy | L | Post-rebuild. Pull blood glucose from Apple Health (incl. CGM sources like Dexcom/Libre that write to Health) and correlate against episode onset/severity. Depends on HealthKit integration above. |
| Platform | Apple Health medication log — write check-in medication entries to HealthKit Medication tracking | Missy | L | Requires native iOS app + HealthKit entitlements. When a user logs medication via Baseline check-in, sync that entry to Apple Health's medication log so it appears alongside other health data. Depends on Apple Health / HealthKit integration above. (Salvaged 7/14/26 from the orphan branch `claude/add-medication-logging-H3lWL`, which had no PR — see Decision Log.) |
| Monetization | Freemium tier definition and paywall | Internal | L | FTC/MHMD compliance required before paid tier |
| Monetization | Sponsored protocol library with clear labeling | Internal | L | Secondary revenue stream |
| Community | Anonymized aggregate experiment outcomes | Internal | L | |
| Protocol Library | Curated protocol templates by condition area (GF, Keto, FODMAP, Vegan etc.) | Internal | L | |

---

## Deferred — Not Building Now

| Area | Item | Rationale |
|---|---|---|
| UX | Self-serve access request form | Superseded 4/14/26 — replaced by self-serve registration with email verification (no access-request step needed). |

---

## Needs Investigation

| Area | Item | Source | Notes |
|---|---|---|---|
| Privacy/Legal | GDPR obligations if non-US users join | Legal | Not immediate — all current users are US-based |

> **Closed 7/14/26 — Future episode dates:** decided *keep blocked* (future-dating isn't a supported case). The block is intentionally coarse (naive local-vs-UTC comparison, fuzzy by the tz offset) — documented in the code at `new_episode`/`edit_episode` so audits don't re-flag it. Exact tz behavior arrives incidentally with the P1 per-user-tz / `onset`-model work; this is **not** a standalone item.

---

## Decision Log

### Structured Triggers — Episode Capture Path (Increment 2)
**July 15, 2026:** Wired the first read/write path for the trigger data model (the schema shipped in Increment 1 with zero rows). Scope was deliberately sliced to **the episode capture path only** (Missy's call) — Settings management, AI check-in parity, and dashboard reads are later increments. Decisions:
1. **`EpisodeTrigger.source` ('user'/'ai', default 'user') added now** (owner-approved). Free while zero rows exist; a backfill-guess migration later. Confirmed it has no Rule 1 (statelessness) implication — it's persisted DB state, not per-server memory (Missy asked). Feeds the future AI trigger-analysis item (weight confirmed vs inferred).
2. **UI = chip multi-select, not a dropdown** (Missy raised the pattern question). Codified in CONVENTIONS.md: dropdown = single-select, pills = multi-select. Native multi-`<select>` is unusable on mobile; chips reuse the binary-toggle's JS `.selected` approach (no `:has()` dependency).
3. **Match-and-link** (`_resolve_trigger`): a typed name resolves to an **active** global (case-insensitive) → else the user's own custom (reactivating a soft-deactivated one) → else a new custom. Reuses the `Symptom` unique-name precedent; prevents split analytics. A typed name matching a **soft-retired** global becomes the user's own custom (agency, no silent drop) rather than resurrecting an admin-retired shared row — resolves an asymmetry Code Review flagged (the checkbox path already refuses to freshly link inactive triggers).
4. **Concurrent same-name race → `begin_nested()` savepoint** inside `_resolve_trigger`, not an outer try/except. Review (QA B1) found the original outer `try: commit` never caught it because `_resolve_trigger` flushes *first*, so the IntegrityError escaped to a 500. The savepoint isolates the race to the row and re-selects the winner, keeping the episode transaction intact — the row-level form of the `_commit_compliance` "retry once" convention.
5. **Edit = replace-on-save** (delete links, re-create from form) matching interventions/symptom-scores, with a server-computed `preserve_ids` allowance so a linked-but-soft-deactivated custom the user leaves checked isn't dropped by the active-only picker validation.

**Review-driven fixes (QA + Code + UX, all 3 blockers fixed & re-verified in a real browser at the write + edit + submit-flush + overflow paths):** (B1) the escaping IntegrityError → savepoint (above); (UX-B1) typed-but-not-"+Add"ed trigger text was silently lost → the episode form now flushes pending input on `submit`; (UX-B2) a long space-free custom name forced horizontal page scroll at 375px → `overflow-wrap: anywhere` + `max-width:100%` on the chip and the history line. Also: chip tap target 40→44px, `.trigger-add-row` converged with `.symptom-add-row` (flex-shrink/min-width), IME-composition guard on Enter. Five lower-risk findings accepted and tracked (see the P2 "deferred review findings" row): non-ASCII case-fold, no `source` CHECK constraint, history N+1, no chip ✕-remove, fieldset/legend + emoji-marker a11y.

### Timezone Bug — `baseline_tz` Cookie Encode/Decode + `user_today()` Single Source of Truth
**July 14, 2026:** Production bug found in smoke testing: after ~5pm Pacific the dashboard "Today's Protocols" card showed today as empty/"Unconfirmed" and edits appeared to revert cross-device. **Root cause (reproduced deterministically):** base.html writes the `baseline_tz` cookie URL-encoded (`encodeURIComponent` → `America%2FLos_Angeles`); Flask returns cookie values un-decoded; `ZoneInfo('America%2FLos_Angeles')` raises `ZoneInfoNotFoundError`; a bare `except: pass` swallowed it → silent UTC fallback. So the dashboard render had **always** used UTC — masked during the day (UTC == Pacific date until ~5pm) and by a client-side dot recolor at save time, surfacing only in the evening. **Not** caused by the trigger deploy; **no data lost** (writes used the browser-local date, so rows sit on the correct days — only the render's "today" was wrong). A full audit of every `date.today()`/`utcnow()`/`now()` found the same class in the detail-form write, `protocol_detail` read, ProtocolEvent dates, and the AI check-in fallback. **Fix:** one `user_today()` helper (unquotes the cookie, falls back to server date) as the single source of truth, wired into every functional day-boundary path; `test_timezone.py` regression suite. Also tightened (same pass): log-day window anchored on `user_today`, support-message rotation on `user_today`, detail-form note 500-cap. Reviewed by QA + Code (0 blockers). **Deferred to the P1 per-user-tz project (owner-approved, Option A):** chart bucketing, form date defaults, future-episode validation, naive `Episode.onset` model, and unifying the card's browser-sent write date with the cookie (A1 dual-source: divergence only on travel / first-request, negligible at 5 same-tz users; durable fix is a stored `User.timezone`). **Lesson:** a bare `except: pass` around timezone parsing turned a config mistake into a silent, ~long-standing UTC fallback — catch narrowly and log, don't swallow.

### React Rebuild Readiness — Backlog Review & Pre-Rebuild Scope
**July 14, 2026:** Full backlog review to answer "what's left before the React rebuild." Sorted every open item by the March 2026 **Build Sequencing** rule — data-model/backend changes land in the monolith first; frontend-only work defers to React (building it in Jinja is throwaway). Headline: **no open P0s or bugs** — this is groundwork sequencing, not blockers.

**Agreed pre-rebuild scope (Missy 7/14/26):** the minimal data-model set + CSRF + staging, defer CI to early-rebuild.
- **Do before rebuild (data-model / backend):** structured trigger tracking (hybrid — see its row); user-timezone support (the stored/detected-tz model change); FK cleanup → DB-level `ondelete`; "date stopped" effective-date field; neurologist insurance report **backend spec only** (build the PDF during the rebuild).
- **Do before, different reason:** CSRF (Flask-WTF) monolith fix — protects the multi-month monolith window + any cookie-based post-rebuild web surface; staging environment on Railway before a big migration.
- **Defer to early-rebuild:** GitHub Actions basic test suite (regression net during migration).
- **Spec now, build in React (no throwaway Jinja):** chart time-range selector; trend-based supportive messages (reuse the message-catalog/adherence-% query, defer the UI); compliance-aware experiment views.
- **Do NOT build in monolith:** configurable reminders, light mode, photo logging (already deferred); pill-styling unification; edit-compliance-older-than-7-days affordance — the rebuild rewrites these anyway.

**CSRF split into two independent items (7/14/26):** (1) P2 monolith Flask-WTF fix — do now, not superseded by the rebuild; (2) rebuild-scoped web-client auth-model decision (header-token vs. cookie+`SameSite`+CSRF tokens) under "Deferred to React Rebuild." Reasoning: CSRF is a cookie-session-in-a-browser problem, not a framework problem. Native App Store (React Native) apps use header-token auth and aren't CSRF-exposed; the React **web app/PWA** — which stays for current users — remains exposed unless cookie sessions are deliberately designed out. The rebuild does not automatically fix it.

**Trigger tracking model → hybrid (7/14/26):** curated global seed list + user-defined custom triggers; one `Trigger` table (nullable `user_id`) + `EpisodeTrigger` junction. Decisions: **global seed list capped at ≤12 items** (curation call resolved — short enough that deferring per-user hide is safe); **per-user add is v1** (create/rename/deactivate own customs) while **per-user hide of globals is v2**; global↔custom name collision resolved by match-and-link to a global (reusing the `Symptom` case-insensitive-unique precedent) to avoid split analytics; **custom deletion is a soft-deactivate** (`is_active=False`), FK-safe, mirroring the soft-retire rule for globals; **two surfaces** — full management on the Settings screen ("Manage triggers") plus an **inline "+ add" on the episode form** for mid-log capture, keeping the main nav clean; forward-only (no historical-note backfill). Aggregate/community analytics (P3) span only globals. See the P1 row for the full sub-decision list.

**Housekeeping:** partial-week chart display confirmed resolved (asterisk label) and removed from CLAUDE.md Known Issues; future-episode-dates investigation closed — keep blocked, not needed.

### Orphan Branch Cleanup — `claude/add-medication-logging-H3lWL`
**July 14, 2026:** While reviewing branches in GitHub, Missy spotted `claude/add-medication-logging-H3lWL` with no corresponding PR. Investigation confirmed a true orphan: no PR was ever opened for it (only PRs #1–#3 exist, all merged, all for other branches), and its single commit (`685f27b`, Apr 9 2026) was never in `main`. Despite the name it contained no app code — just one docs-only commit adding a P3 backlog item ("Apple Health medication log sync"). It had been branched off a *local* merge commit that itself never reached main (main took that work via PR #1's merge instead), so it forked ~3 months ago on a stale base. The one item of value — the medication-log-sync idea, absent from main's backlog (which had general HealthKit + HRV + glucose correlation, but not the outbound write-to-Apple-Health item) — was salvaged into the Platform section here rather than PR'd from the stale branch. The orphan branch was then deleted. Lesson: branches created outside the PR flow (or from local merges) can strand real work; periodic branch review catches them.

### Review-Process Hardening — Retrospective on the 7/12 Smoke-Test Misses
**July 12, 2026:** Retrospective on why the QA/Code/UX review agents missed the issues Missy caught in production smoke testing (FK delete crash; dot-strip navigation and stacked-editor state bugs). Root causes: (1) reviews were diff-scoped, but the compliance feature changed *data density*, which changed the behavior of unchanged code (delete paths); (2) local SQLite didn't enforce FKs, so the one environment where the delete bug existed was production; (3) static reviewers can't click — multi-step state-machine bugs are invisible without walking sequences; (4) review prompts described intended behavior, inviting confirmation instead of falsification; (5) a QA finding that anticipated the stacked-editor bug was triaged as "pre-existing, out of scope" and footnoted instead of promoted. Changes shipped: `PRAGMA foreign_keys=ON` for local SQLite (database.py); blast-radius sections added to QA + Code Reviewer checklists; adversarial-sequence sections added to all three reviewer checklists and to the Phase-1 verification step in CLAUDE.md ("users rarely follow happy paths" — Missy); falsify-don't-verify rule in all reviewer instructions and prompt-framing guidance; triage rule promoting pre-existing findings on newly-prominent surfaces to explicit deploy-gate decisions.

**The retooled process proved itself in its own review pass.** My initial delete-path audit claimed all paths clean; the reviewers running the new blast-radius/falsify instructions immediately found two live production bugs the audit missed: (1) deleting an episode logged via AI check-in 500'd on the `checkins.episode_id` FK (QA reproduced it) — fixed by detaching check-in messages (`episode_id=NULL`, chat history survives) with IntegrityError rollback; (2) `cleanup_stale_unverified_users()` had silently failed on every startup since analytics shipped — the signup `UserActivity` row blocked the User delete and a bare no-logging `except` swallowed it, meaning **unverified accounts were never actually deleted** despite the documented 48h policy — fixed by anonymizing activity rows (`user_id=NULL`, signup analytics stay accurate) and per-user commit with logged failures. Both fixes verified under FK enforcement.

### Protocol Delete — Detach Experiments, FK-Safe Children Cleanup
**July 12, 2026:** Production smoke test after the Today's Protocols deploy surfaced an IntegrityError on protocol delete — `delete_protocol` never removed ProtocolCompliance/ProtocolEvent children or detached Experiments, and PostgreSQL enforces those FKs (SQLite locally does not, so it always passed local testing). Pre-existing bug; the new card made it near-certain to fire because every Confirm now writes compliance rows for all active protocols. Decisions: (1) Experiments referencing a deleted protocol are **detached** (`protocol_id=NULL`), not deleted — hypothesis/outcome/decision history survives; all templates already guard `{% if exp.protocol %}`. Considered blocking the delete (contradicts soft-warnings philosophy) and cascading (destroys the user's science). (2) Compliance/event rows delete with the protocol — they're protocol-scoped logs. The crash had been accidentally protecting this history, so the confirm dialog now explicitly warns it's permanent; a richer guard (history count or archive pattern) is a P2 decision. (3) IntegrityError → rollback + friendly flash for defense in depth. Testing note recorded: delete paths must be reasoned against PostgreSQL FK semantics, not just local SQLite.

### Today's Protocols — Daily Compliance Card, Backfill, "Why" Field & AI Parity
**July 12, 2026:** Shipped the compliance pair (manual logging + UX rework) as one feature, designed first in the Claude Design project ("Baseline Design System" → Compliance groups) and iterated with Missy there before any code. Decisions, each raised with short-term vs long-term trade-offs:
1. **Unique index now, not later** (Missy's call, against the defer recommendation): dedup pass + `CREATE UNIQUE INDEX IF NOT EXISTS ux_protocol_compliance_day` on `protocol_compliance(user_id, protocol_id, date)` in `run_migrations()`, log-and-continue so a failure can't block startup; model-level UniqueConstraint added for fresh DBs. Both statements valid on SQLite and PostgreSQL.
2. **JSON fetch endpoint** (`POST /protocols/log-day`) over form-post: no reload/scroll loss for confirm + backfill, survives the API-first React rebuild, natural shape for the future MCP layer. One endpoint serves both today-confirm and backfill (same operation, different date).
3. **Server-side message catalog, pre-interpolated at render:** single Python source of truth for supportive copy (`SUPPORT_MESSAGES`); the pre-request pill-flip moment reads from JSON embedded in the page. Deterministic daily rotation. `str.replace` interpolation (user text may contain braces). Future trend messages slot into the same catalog.
4. **Folded the old "Active Protocols" dashboard list** into the card ("keep it simple"); "Manage →" link preserves the path.
5. **AI check-in full parity:** schema changed from "list of IDs taken" to `{id, took, note}` objects (legacy bare ints still parsed); writes now upsert via the shared `_upsert_compliance()` helper, so the most recent explicit statement wins in both directions. The AI preserves an existing user note when it has nothing to say about it.
6. **Assumed-taken with distinct styling:** pills default to Taken but in a dashed/soft "our suggestion" style to counter default bias — the user's Confirm converts them to solid recorded fact. Nothing writes until Confirm.
7. **Deliberate asymmetry:** today defaults assumed-taken; backfill days default neutral with tap-to-clear ("a blank day is better data than a guess"). Backfill window is 7 days; dot strip never shows red (fully-missed-but-logged = amber; only the Missed pill itself is red).
8. **Timezone interim fix** (raised by Missy): browser-local date sent with all compliance writes (dashboard fetch body; check-in's existing `client_time` field), server validates ±1 day of UTC today. After QA review flagged the render/write mismatch, the card's server-side render was also localized via a `baseline_tz` cookie (IANA zone set by base.html, read with `zoneinfo` in `index()`, UTC fallback). Charts still use server date. Full user-timezone support added to backlog as P1.
9. **Review-driven hardening (same session):** `_commit_compliance()` retries once on IntegrityError so concurrent confirms can't silently lose a write (QA-reproduced race); AI check-in commit degrades to a "please resend" flash instead of a 500; AI compliance allow-list scoped to `type='preventative'`; malformed JSON entries return 400; note input fixed for iOS auto-zoom (16px) and given an aria-label; today's dot demoted from button to status indicator; quiet text buttons padded to real touch targets; in-flight "Saving…" button states.

### Today's Protocols — Tri-State Partial Logging & Un-Log (revisiting "Assumed Taken")
**July 14, 2026:** Missy reported that logging one preventative protocol and leaving the others blank marked *all* of them taken on Confirm, with no way back to unmarked — she takes protocols morning and night and wanted to log just what she'd completed. Root cause was the intended "assumed compliance" behavior (`took: state.took !== false` wrote every blank as taken) combined with flip-only pills (no clear) and no delete path. Because the fix overrides the documented "assumed compliance" philosophy, it was raised to Missy before building; she specified the design. Decisions:
1. **State-driven button, not a philosophy change.** "Assumed compliance" is preserved as the *default fast path*: when nothing is explicitly marked the button reads **"Confirm all as complete"** (one tap writes all taken, unchanged). The moment anything is marked or cleared it becomes **"Save"**, which writes exactly what's set and leaves blanks unlogged. This keeps the one-tap happy path while making partial logging honest — the app no longer invents "taken" for protocols the user didn't touch once they've started marking.
2. **Tri-state pills with tap-again-to-clear** on the today card (matching the backfill editor), replacing flip-only. The old dashed "assumed-taken" pre-selection styling was removed so a blank pill reads as genuinely neutral; the assumption now lives only in the button label. `.tp-pill-assumed` CSS deleted.
3. **Un-log = delete the row.** `POST /protocols/log-day` `took` became tri-state (`true`/`false`/`null`); `null` routes through a new `_delete_compliance()` in `_commit_compliance()`, so a protocol can return to unmarked even after it was confirmed. Deletes are idempotent → safe on the existing IntegrityError retry. Backfill Save also sends tri-state so clearing a past entry removes it. Validation rejects non-bool/non-null `took` with 400 before any write (no partial writes); the shared `_upsert_compliance()` and its other callers (protocol-detail form, AI check-in) still pass only bools and are unaffected.
4. **Relabel Taken/Missed → "Complete"/"Not Today"** (owner's call) to remove medication bias — protocols include lifestyle/dietary/routine changes, not just meds. Applied to today rows, backfill editor, bulk button ("Mark all Not Today"), done-state summary, dot aria-labels, and help copy.
5. **Partial save is a first-class state:** leaving some protocols blank keeps the card expanded with a "Partly logged" chip and "Saved. The rest are still open for today." — supports the morning-then-night logging rhythm.
6. **Accepted edge (flagged at deploy):** clearing *every* protocol flips the button back to "Confirm all as complete", so the primary button can't clear a whole day to hollow in one press (all-blank is defined as the confirm-all shortcut, per spec). Un-logging works whenever at least one protocol stays marked. A dedicated "clear today's log" affordance is a possible future add if this friction shows up in use.
Verified with a Flask test-client suite (confirm-all, partial, un-log, invalid-type 400/no-partial-write, note validation + preservation, day_status recompute) and a real-browser Playwright pass at 375px driving the card end-to-end (bug reproduction confirmed fixed, tap-to-clear, un-log-one-keep-another, note-clears-on-clear, no label overflow).

**Review-driven hardening (same session, QA + Code + UX agents):** (a) clearing a "Not Today" pill now also clears its note field so a typed note can't silently ride onto a Complete/blank state or be discarded on confirm-all; (b) the backfill editor carries each protocol's existing note through unchanged on Save (`bfNote`) — previously it resent `note:''` for every protocol, which could blank out a note set via the detail form or AI check-in; (c) non-string `note` in the JSON body now returns 400 instead of a 500; (d) `protocol_detail.html` relabeled Taken/Missed → Complete/Not Today (log form + history timeline) so both compliance entry points share one vocabulary; (e) per-protocol `aria-label`s on the pills (today + backfill); (f) focus moves to the "change something" link after a full save collapses the card; (g) a saved-but-incomplete day shows a "Partly logged" chip on reload, not the generic default.

**Deploy-Gate decisions (resolved by Missy 7/14/26):**
1. **Partial-day dot color → Option A (chosen).** All three reviewers flagged that a partially-logged today (e.g. 1 of 3 Complete, rest left blank) rendered a **green** dot labeled "all complete" while the card said "Partly logged" — because the old 7/12 rule "partial-but-all-taken counts as green (positive framing)" now fired routinely on today. Fixed: green is awarded only when *every* active protocol for the day is logged and Complete; a partial-no-miss day is now **amber**. Implemented via a single `_compute_day_status()` helper shared by the server render and the AJAX recompute (also closes the render/AJAX divergence Code Review raised). Overrides the 7/12 positive-framing decision, with the owner's approval. Dot aria-labels + help copy updated.
2. **Concurrent clear-vs-write lost update → accepted + documented.** `_delete_compliance` racing a concurrent stale `took=true` write isn't caught by the IntegrityError retry (delete raises no IntegrityError). Low frequency at 5 users; revisit with row-versioning/optimistic-locking at scale.
3. **Dedup migration tie-break (pre-existing) → flagged, not changed.** The 7/12 `run_migrations()` dedup keeps `MIN(id)` (oldest) rather than `MAX(id)` (newest), contradicting "most recent wins." It already ran in production behind `IF NOT EXISTS`, so re-running won't re-dedup — left as-is; do a one-time prod check (`GROUP BY user_id,protocol_id,date HAVING COUNT(*)>1`) and use `MAX(id)`/`MAX(created_at)` if a similar migration is ever written again.

### Binary Tracked-Item Storage — Separate Column vs Reused `score`
**June 2026:** For the Yes/No tracked-item type (Spec 01), chose to add a new nullable `value_bool` column on `SymptomScore` rather than overloading the existing `score` column with 0/1. Trade-off considered: reusing `score` would have saved a column add but creates a real footgun — any aggregate that sums or averages `score` without joining `Symptom` to check `input_type` would treat a binary `1` as a scale `1`, silently corrupting trend lines and correlations. With separate columns, every existing scale aggregate keeps working unchanged because `score IS NOT NULL` naturally excludes binary rows, and the same shape extends cleanly to the future categorical/count types the spec calls out. Cost: one nullable column on the entries table and slightly more code on the write path. Worth it. Decision is per-spec, not per-symptom — `Symptom.input_type` remains the single source of truth for which column to read.

### Architecture — Monolith First
**March 2026:** Chose monolith-first (Flask + Jinja2) for MVP speed. PWA added for home screen install — solves "I only use apps" adoption objection without native app cost. React frontend rebuild planned post-vacation (April/May 2026) once product stabilizes at 20-30 users. React Native follows React rebuild naturally. All backend work is reusable regardless of frontend choice.

### Architecture — MCP Server / Agent-Accessible Backend
**March 2026:** The emerging paradigm of "software built for agents not humans" — AI assistants (Claude, ChatGPT) access app functionality on behalf of users rather than users opening apps directly. Decision: build both paths. Keep PWA for current users (non-technical chronic illness patients). Add MCP server layer during/after React rebuild exposing core functionality as agent-callable tools: log episode, add protocol, start experiment, query dashboard data. Rationale: apps without agent access will be invisible to power users within 12-18 months. Mainstream UI replacement is 3-5 years out. React rebuild's API-first architecture positions Baseline for MCP with minimal additional work.

### Analytics
**March 2026:** Deferred third-party analytics (Mixpanel, PostHog cloud) until privacy policy is live and Washington MHMD compliance is understood. Health data + behavioral data sent to third parties requires explicit consent. Interim: internal PostgreSQL event logging. PostHog self-hosted is preferred path when ready.

### Symptom Limit
**March 2026:** 3-symptom limit is an onboarding guardrail only — not enforced app-wide. Protects new users from overwhelm without restricting power users.

### Data Deletion
**March 2026:** Account deletion implemented (/settings/delete-account). Satisfies Washington MHMD right to delete. Requires typing "DELETE" to confirm. Deletes all data in FK-safe order: EpisodeInterventions → SymptomScores → EpisodeTriggers → CheckIns → Episodes → ProtocolCompliance → ProtocolEvents → Experiments → Protocols → Symptoms → Triggers (custom only; globals have user_id NULL and are shared, never per-user deleted) → UserActivity → InviteCode reference → User.

### Boolean Migration Defaults — PostgreSQL Compatibility
**March 2026:** PostgreSQL rejects `DEFAULT 0` / `DEFAULT 1` for BOOLEAN columns (requires `DEFAULT FALSE` / `DEFAULT TRUE`). SQLite accepts both. Fixed all ALTER TABLE migrations in `run_migrations()` to use `TRUE`/`FALSE`. Original bug surfaced when `has_seen_tour` migration ran on production PostgreSQL for the first time.

### Welcome Email — smtplib over Flask-Mail
**March 2026:** Chose Python stdlib `smtplib` over Flask-Mail for welcome emails. No new dependency. Gmail App Password for auth, `smtp.gmail.com:587` with TLS, 10-second timeout. Fails silently if `MAIL_USERNAME`/`MAIL_PASSWORD` not set (local dev). HTML email with plain text fallback, dark theme matching app. Sent on registration after `db.session.commit()`.

### Welcome Modal — Guided Tour
**March 2026:** Added 5-step guided walkthrough modal on first dashboard visit after onboarding. `has_seen_tour` boolean on User model. Auto-marks as seen via JS fetch to `/tour/complete`. Replayable from Help page via `/tour/restart`. No external dependencies — pure CSS/JS modal.

### Build Sequencing — Monolith vs React Rebuild
**March 2026:** Reordered P1 build sequence based on throwaway work risk and data model stability. Key decisions: (1) Naming overhaul must happen first — every feature built on old naming creates rework. (2) Data model changes (multiple rescues, compliance logging) should happen in monolith before React rebuild to avoid mid-migration complexity. (3) Neurologist PDF deferred to post-React rebuild — PDF generation layer will be significantly cleaner in API-first architecture. (4) Reminders, light mode, and photo logging deferred to React/React Native — building these in the monolith creates throwaway frontend work.

### Naming & Market Positioning
**March 2026:** Deliberately positioning Baseline beyond medication management to include health optimization protocols (morning routines, cold plunge, sleep hygiene, dietary approaches). Influenced by Huberman Protocol cultural momentum and user feedback from Kiersten (perimenopausal tracking doesn't fit "symptoms" or "preventative medication" framing). Final naming decisions: Symptoms → "What I Track", Rescue Medications → "Interventions", Protocols description updated to be lifestyle-inclusive. "Episodes" retained — works for both communities.

### Experiment/Protocol Workflow Redesign
**April 2026:** Resolved circular dependency in protocol/experiment creation. Previously, starting an experiment required an existing protocol, but creating a protocol immediately redirected to experiment setup — confusing flow. Fix: (1) Added "+ Add new protocol" option directly in the experiment form's protocol dropdown, with inline fields for protocol name and dose/frequency. (2) Experiment name auto-suggests from protocol name (e.g. "Magnesium 400mg trial") but remains editable. (3) Added active experiment warning modal on the experiment form — fires before submit, matching the existing pattern on the protocol form. Help page updated to lead with experiment-first flow.

### Pre-LinkedIn Launch Requirements
**March 2026:** Identified minimum viable requirements before posting on LinkedIn to 1000+ connections: privacy policy in app (✅ complete), in-app help/tutorial, welcome email, Baseline email address (✅ baselinehealthapp@gmail.com). Invite-only registration maintained during this phase. Self-serve access request deferred until support infrastructure is ready.

### Email Sending — Resend API over Gmail SMTP
**April 14, 2026:** Migrated transactional email from Gmail SMTP (smtplib) to Resend API after verification emails failed silently in production. Root cause: Railway blocks outbound SMTP (errno 101 network unreachable). Resend uses HTTPS, works from Railway. From address hardcoded to `Baseline <hello@mybaselineapp.com>` — requires domain verification in Resend. New env var `RESEND_API_KEY` replaces `MAIL_USERNAME`/`MAIL_PASSWORD`. Local dev still fails silently when key unset. HTML + plain text email content unchanged.

### Email Opt-In, Unsubscribe & Admin User View
**April 24, 2026:** Added `email_updates_enabled` boolean (default True) to User and shipped:
(1) Settings → Email Preferences toggle (auto-saves on change — no Save button needed; mirrors AI Logging toggle behavior).
(2) Welcome email carries an unsubscribe link in its footer; verification email stays purely transactional.
(3) `GET /unsubscribe/<token>` is publicly accessible — token is a stateless HMAC-SHA256 over `SECRET_KEY` (salt `baseline-email-unsubscribe-v1`, no expiry by design — old emails should still work years later). Trade-off: rotating `SECRET_KEY` invalidates all in-flight tokens; affected users must use Settings.
(4) Resend audience contacts kept in sync on verify, settings toggle, email change, unsubscribe, and account delete — gated on `RESEND_AUDIENCE_ID`, log-and-swallow on failure, contacts keyed by email (not stored ID).
(5) Read-only `/admin/users` directory (email, verified, joined, last login, email-updates state, episode count) guarded by `user.is_admin`. Unauthorized access logs a warning. Excluded from analytics tracking.
(6) Admin link surfaced in Settings (above Account, only when `is_admin`) — replaces the previous "only via sidebar" pattern.

Decisions:
- Reused existing `is_admin` flag instead of hardcoded email check on `/admin/users` for consistency with `/admin/analytics`.
- Chose HMAC-SHA256 (per spec) over itsdangerous unsigned-serializer; same security, simpler.
- Kept episode count in admin view despite open registration — single aggregate integer (engagement signal) rather than health content. Privacy policy explicitly discloses admin can see this.
- Removed the `ANTHROPIC_API_KEY` env-var diagnostic from user-facing Settings (was leaking dev info to non-developer users).

Known follow-ups: CSRF on `/settings/email-preferences` rolls into the existing site-wide CSRF backlog item.

### Self-Serve Registration with Email Verification
**April 14, 2026:** Replaced invite-only registration with self-serve registration gated by email verification. Chose email-verification-only (vs. fully open or invite+verify) to support LinkedIn launch without manual invite issuance. Implementation: itsdangerous signed tokens (HMAC over SECRET_KEY, 24h TTL) sent via existing Gmail SMTP pipeline; SHA-256 hash of consumed tokens stored in `used_verify_tokens` to prevent replay (chosen over "verified_at check only" for defense in depth); Flask-Limiter (in-memory) applied to `/register` 5/hour, `/resend-verification` 5/hour, `/login` 20/hour; small hardcoded disposable-email blocklist; required privacy-policy acknowledgment checkbox on the form (server-enforced); stale unverified accounts deleted at startup after 48h so the email can be re-registered. Login leaks no password-validity oracle for unverified accounts (unverified banner shown before bcrypt check). Welcome email moved from registration to post-verification. `InviteCode` model retained for admin use via `/dev/create-invite`. Known follow-ups in backlog: CSRF protection (pre-existing gap), Redis backend for rate limiter. In-app privacy policy (`templates/privacy.html`) updated in the same commit to describe transactional email (verification + welcome), 48-hour unverified-account retention, and the shift away from invite-only.

---

## Sources
- **Lizz** — engineer user, security and data integrity feedback
- **Mackenzie** — stepdaughter, chronic illness user, UX and feature feedback
- **Kiersten** — cousin, user
- **Katherine** — Kiersten's daughter, user
- **Dave** — partner, Android PWA testing
- **Missy** — product owner
- **Internal** — product/engineering decisions
- **Legal** — compliance requirements
